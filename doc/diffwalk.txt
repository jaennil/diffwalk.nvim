*diffwalk.txt*  Walk a diff in the file itself, navigate from a panel

==============================================================================
CONTENTS                                                     *diffwalk-contents*

    1. Introduction ......................... |diffwalk-introduction|
    2. Requirements ......................... |diffwalk-requirements|
    3. Commands ............................. |diffwalk-commands|
    4. Panel ................................ |diffwalk-panel|
    5. Configuration ........................ |diffwalk-config|
    6. Lua API .............................. |diffwalk-api|
    7. Caveats .............................. |diffwalk-caveats|

==============================================================================
1. INTRODUCTION                                          *diffwalk-introduction*

diffwalk highlights a change in the file you already work in, using gitsigns,
and gives you a panel that is nothing but navigation: one line per file, one
line per hunk. Your LSP, your jumps and your keymaps keep working, because you
never leave the real buffer.

Two things can be walked: the current branch against the default branch, and
a single commit against its parent.

==============================================================================
2. REQUIREMENTS                                          *diffwalk-requirements*

    - Neovim 0.10 or newer
    - gitsigns.nvim, which draws the in-file highlights
    - git in $PATH

==============================================================================
3. COMMANDS                                                  *diffwalk-commands*

                                                              *:DiffwalkBranch*
:DiffwalkBranch         Hunks between the merge base with the default branch
                        and the working tree. The merge base is used so that
                        commits landed upstream after you branched stay out of
                        the diff.

                                                             *:DiffwalkCommits*
:DiffwalkCommits [n]    List the last [n] commits (default: |diffwalk-config|
                        `commits`). <CR> walks the diff of the commit under
                        the cursor, <BS> comes back to the list.

                                                              *:DiffwalkCommit*
:DiffwalkCommit {rev}   Walk the diff of {rev} against its parent. A root
                        commit is diffed against the empty tree.

                                                               *:DiffwalkFiles*
:DiffwalkFiles          Put the changed files of the branch in the quickfix
                        list and set the diff base.

                                                                 *:DiffwalkOld*
                                                                *diffwalk-old*
:DiffwalkOld            Open the current file as it looks at the diff base, in
                        a split beside it, with both sides in diff mode.

                        Removed lines are virtual lines and virtual text
                        cannot hold the cursor, so this is how a deleted
                        function signature gets read, searched or yanked. The
                        buffer is not a file on disk: treesitter highlights
                        it, but no LSP attaches. |diffwalk-config| `keys.close`
                        closes it and turns diff mode off again.

                                                       *:DiffwalkToggleDeleted*
:DiffwalkToggleDeleted  Show or hide removed lines in the file buffers. They
                        are virtual lines, so they push real code down.

                        diffwalk draws them itself: gitsigns anchors its own
                        to the left edge of the window, which shifts them out
                        of line with the code by the width of the sign column,
                        and virtual text does not expand tabs. Here they are
                        expanded against the buffer's tabstop and padded to
                        the window, so the indent matches what is beside them.

                                                               *:DiffwalkReset*
:DiffwalkReset          Drop the diff base, every highlight it turned on and
                        the viewed marks.

==============================================================================
4. PANEL                                                        *diffwalk-panel*

A file line carries the path and the counts, a hunk line the line number, the
kind of the first change and its text: >

    lua/jaennil/plugins/gitsigns.lua  +25 -0
         9  + on_attach = function(bufnr)
<
Default mappings inside the panel:

    <CR>        show the hunk, the cursor stays in the list
    o           show the hunk and jump into the file
    ]f          next file
    [f          previous file
    x           mark as viewed; on a file line, the whole file
    a           show all hunks, or only the unviewed ones
    <BS>        back to the commit list
    q           close the panel

                                                             *diffwalk-viewed*
A viewed hunk carries a check mark and is dimmed, and a file line counts how
many of its hunks are done: >

    ✓ lua/jaennil/plugins/gitsigns.lua  +25 -0  1/1
    ✓      9  + on_attach = function(bufnr)
      lazy-lock.json  +15 -11  0/2
           2  - "fidget.nvim": { "branch": "main", ...
<
The added lines are painted by diffwalk, not gitsigns: gitsigns diffs in the
background, which left a file showing up uncolored for a moment after it was
opened, and it does not attach at all to a file that is absent from the base,
which left a newly added file looking unchanged.

What is painted comes from the hunks gitsigns holds for the buffer, which it
keeps in step as the buffer is edited, so the highlights follow the code
rather than a snapshot taken when the review started. A file gitsigns will
not attach to falls back on that snapshot.

A marked hunk is dimmed in the file as well: its changed lines drop from the
review palette to `DiffwalkViewed` and get a check mark in the sign column,
drawn above the gitsigns ones. Only the changed lines are dimmed, never the
context around them; deleted lines stay red, since those are virtual lines and
the check mark beside them is the marker.

Marking is manual: `x` on a hunk line, or on a file line for all of its hunks.

A mark is keyed by the diff base, the file and a hash of the hunk's own patch
text, never by its line number: line numbers move as soon as anything above
them is edited. The marks are written to stdpath("state")/diffwalk, one file
per repository, so a review survives closing Neovim. Editing a hunk that was
marked makes it a different hunk, and it comes back unmarked.
|:DiffwalkReset| drops them.

==============================================================================
5. CONFIGURATION                                               *diffwalk-config*

setup() is optional. Defaults: >

    require("diffwalk").setup({
      remote = "origin",
      branches = { "master", "main" },
      fetch = true,

      commits = 100,
      position = "right",
      size = 60,

      linehl = true,
      deleted = true,
      word_diff = true,
      vertical = true,
      edges = true,
      persist = true,

      colors = {
        added = "#005f00",
        added_word = "#008700",
        added_sign = "#5fd75f",
        removed = "#5f0000",
        removed_word = "#870000",
        removed_sign = "#ff005f",
        viewed = "#1c2a1c",
        viewed_sign = "#5f875f",
      },

      keys = {
        open = "<CR>",
        focus = "o",
        next_file = "]f",
        prev_file = "[f",
        mark = "x",
        filter = "a",
        back = "<BS>",
        close = "q",
      },
    })
<
remote          Remote the default branch is looked up on.

branches        Fallbacks used when the remote has no HEAD symref. Set it up
                once with `git remote set-head origin -a` and this is skipped.

fetch           Fetch the default branch before computing the merge base. Set
                to false to work offline against what you already have.

colors          The defaults are the saturated 256-color diff palette, not the
                theme's DiffAdd and DiffDelete: those sit a couple of shades
                above the background and lose against syntax highlighting.

                They are applied to the gitsigns groups, including the word
                diff ones, which otherwise link to TermCursor (reverse video)
                and show up as bright blocks. Inside a fully deleted line the
                word diff is flattened to the line color, since there it would
                only cover ragged chunks of leading whitespace.

edges           Whether a hunk is bracketed in the sign column as `┌ │ └`
                rather than drawn as a plain bar. The bracket sits beside the
                line numbers, so the start and the end of a hunk are visible
                without a separator row cutting through the code.

                The sign column is drawn by diffwalk, and the gitsigns signs
                are turned off while a review is on, so the two never fight
                over it.

                The bracket runs through the removed lines as well. Those are
                virtual lines, which have no sign column, so their gutter is
                drawn as part of the line and padded to the width the real
                one takes: the count of the number column, two columns for the
                signs and the fold column. That width is worked out rather
                than read back, because with signcolumn=auto the column is
                still empty at the moment the marks are placed.

persist         Whether the viewed marks are kept on disk between sessions.

position        Where the panel goes: "right", "left", "bottom" or "top".

size            Columns when the panel is on a side, lines otherwise.

vertical        Whether :DiffwalkOld splits vertically.

keys            Set an entry to false or "" to leave it unmapped.

==============================================================================
6. LUA API                                                        *diffwalk-api*

                                                            *diffwalk.branch()*
require("diffwalk").branch()

                                                             *diffwalk.files()*
require("diffwalk").files()

                                                           *diffwalk.commits()*
require("diffwalk").commits([limit])

                                                            *diffwalk.commit()*
require("diffwalk").commit({rev} [, {back}])
                        {back} is called when <BS> is pressed in the panel.

                                                               *diffwalk.old()*
require("diffwalk").old()

                                                    *diffwalk.toggle_deleted()*
require("diffwalk").toggle_deleted()

                                                             *diffwalk.reset()*
require("diffwalk").reset()

==============================================================================
7. CAVEATS                                                    *diffwalk-caveats*

<CR> opens the current version of the file on disk. For a branch review that
is exact. For an older commit the line numbers drift as far as the file has
moved since that commit, and a file deleted in the working tree is reported
instead of opened.

vim:tw=78:ts=8:ft=help:norl:
